# Railway/Nixpacks builds run with NODE_ENV=production, which makes npm skip
# devDependencies — but the build itself needs them (nest CLI, typescript).
# `include` takes precedence over `omit`, so this keeps builds working without
# depending on a dashboard-level env var.
include=dev
